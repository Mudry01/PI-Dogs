const SearchBar = () => {
    return (
        <div>
          <input
            type="search"
            placeholder="Search by name..."
          />
          <button>
            Search Doggy
          </button>
        </div>
      );
};

export default SearchBar;